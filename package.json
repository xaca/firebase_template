{
  "name": "codespaces-blank",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html",
    "prod": "npm run clean && parcel build src/index.html --dist-dir public && firebase deploy",
    "clean": "rimraf public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "firebase": "^10.12.0",
    "rimraf": "^5.0.5"
  }
}
